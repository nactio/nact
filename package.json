{
  "name": "nact",
  "version": "0.1.0",
  "description": "Mulithreaded implementation of the actor model for node",
  "main": "index.js",
  "scripts": {
    "build": "babel src -d lib",
    "build-test": "babel test -d compiled_test",
    "test": "yarn run build && yarn run build-test && mocha --require babel-polyfill  ./compiled_test/**/*.js"
  },
  "author": "Nick Cuthbert (https://github.com/ncthbrt)",
  "license": "Apache-2.0",
  "dependencies": {
    "bluebird": "^3.5.0",
    "webworker-threads": "^0.7.13"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-polyfill": "^6.26.0",
    "babel-preset-env": "^1.6.0",
    "babel-preset-stage-3": "^6.24.1",
    "chai": "^4.1.1",
    "chai-as-promised": "^7.1.1",
    "mocha": "^3.5.0"
  }
}
